<?xml version='1.0' encoding='utf-8'?>
<odoo>
<record id="view_partner_property_account_bookkeeper" model="ir.ui.view">
    <field name="name">res.partner.property.account.bookkeeper</field>
    <field name="model">res.partner</field>
    <field name="inherit_id" ref="account.view_partner_property_form"/>
    <field name="arch" type="xml">

        <!-- نبحث عن الـ group الأصلي -->
        <group name="accounting_entries" position="attributes">
            <!-- نضيف مجموعة Bookkeeper -->
            <attribute name="groups" add="account.group_account_user"/>
        </group>

    </field>
</record>
</odoo>